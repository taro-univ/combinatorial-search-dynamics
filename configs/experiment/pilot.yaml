name: pilot
phase: 2
description: Deterministic CPU-only pipeline validation; not a research experiment.
instance_count: 9
seed:
  instance_generation: 100
  llm_sampling: 200
  data_split: 300
  representation: 400
  dynamics: 500
  optuna: 600
  bootstrap: 700
